---
# override/additions to docker-stack.yml for deploying to the diagnostic swarm
version: '3.7'
services:
  pfm-web:
    secrets:
      - source: diagnostic.riffplatform.com.key.11
        target: site.key
      - source: diagnostic.riffplatform.com.crt.11
        target: site.crt

  pfm-riffdata:
    configs:
      # override the normal config defined in docker-stack.yml so that it can be replaced
      - source: riffdata.local-production.yml.1
        target: /app/config/unused1
      # diagnostic riffdata specific config replaces the above default config
      - source: riffdata.local-production.yml.2
        target: /app/config/local-production.yml

  pfm-riffrtc:
    configs:
      # override the normal config defined in docker-stack.yml so that it can be replaced
      - source: riff-rtc.local-production.yml.9.debug
        target: /app/config/unused1
      # diagnostic riff-rtc specific config replaces the above default config
      - source: riff-rtc.diag-local-production.yml.3
        target: /app/config/local-production.yml

# Your cert will expire on 2022-07-18 (diagnostic key.11 & crt.11)
secrets:
  diagnostic.riffplatform.com.key.11:
    external: true
  diagnostic.riffplatform.com.crt.11:
    external: true

configs:
  riffdata.local-production.yml.2:
    external: true
  riff-rtc.diag-local-production.yml.3:
    external: true
