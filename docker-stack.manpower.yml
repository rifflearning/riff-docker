---
# override/additions to docker-stack.yml for deploying to the manpower (riffremote 1) swarm
version: '3.7'
services:
  pfm-web:
    secrets:
      - source: manpower.riffremote.com.key.2
        target: site.key
      - source: manpower.riffremote.com.crt.2
        target: site.crt

  pfm-riffrtc:
    configs:
      # override the normal config defined in docker-stack.yml so that it can be replaced
      - source: riff-rtc.local-production.yml.8.debug
        target: /app/config/unused1
      # manpower riff-rtc specific config replaces the above default config
      - source: riff-rtc.manpower-local-production.yml.2
        target: /app/config/local-production.yml

# Your cert will expire on 2020-09-23 (manpower key.2 & crt.2)
secrets:
  manpower.riffremote.com.key.2:
    external: true
  manpower.riffremote.com.crt.2:
    external: true

configs:
  riff-rtc.manpower-local-production.yml.2:
    external: true
